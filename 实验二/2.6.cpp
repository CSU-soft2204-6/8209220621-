#include<iostream>
using namespace std;
/*int main()
{
	int a, b,c,d;
	cin >> a >> b;
	for (c = min(a, b);; c--)
		if (a % c == 0 && b % c == 0)
		{
			cout << "最小公约数为" << c << endl;
			break;
		}
	for(d=max(a,b);;d++)
		if (d % a == 0 && d % b == 0)
		{
			cout << "最大公倍数为" << d << endl;
			break;
		}
}
*/