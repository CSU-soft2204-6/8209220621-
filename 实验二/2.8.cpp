#include<iostream>
#include<cmath>
#include<iomanip>
using namespace std;
/*int main()
{
	double a,xn,xn1;
	cin >> a;
	if (a >= 0)
	{
		xn = a;
		xn1 = (xn + a / xn) / 2;
		while (fabs(xn1 - xn) >= 0.0000000001)
		{
			xn = xn1;
			xn1 = (xn + a / xn) / 2;
		}
		cout <<fixed<<setprecision(10)<< xn1;
	}
	if (a < 0)
		cout << "error";
}
*/